
function sumar(numero1, numero2) { // parametros (cuerpo de la funcion)
    console.log( numero1 + numero2);

}

sumar(30, 5); // argumentos o valores reales
sumar(30, 65); // argumentos o valores reales
sumar(30, 3); // argumentos o valores reales
sumar(30, 34); // argumentos o valores reales

const sumar2 = function(numero1, numero2) {
    console.log(numero1 - numero2);
}

sumar2(50, 6);


